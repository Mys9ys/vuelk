/* eslint-disable */
import axios from "axios";

import {baseConfig} from "@/store/config";

export const recoverModule = {
    state: () => ({
        recoverMailData: {
            type: null,
            mail: null,
        },
    }),
    getters: {},
    mutations: {

    },
    actions: {}
}